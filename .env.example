# Port the server listens on (host side for Docker, or direct for native)
PORT=8080

# MongoDB connection — change if using a remote/auth-enabled instance
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=alerts_db

# Allowed CORS origin (leave empty to default to the same host)
CORS_ORIGIN=
